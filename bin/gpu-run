#!/usr/bin/env bash
# Run gpu-run straight from a source checkout, without installing the package.
set -euo pipefail
ROOT="$(cd "$(dirname "$(readlink -f "$0")")/.." && pwd)"
exec "${GPU_KEEPALIVE_PYTHON:-python3}" -c 'import sys; sys.path.insert(0, sys.argv.pop(1)); from gpu_keepalive.gpu_run import main; sys.exit(main())' "$ROOT" "$@"
